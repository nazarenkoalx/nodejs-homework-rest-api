const { contactsSchema, updateFavoriteSchema } = require("./contactsSchema");
const userSchema = require("./usersSchema");
const subscriptionSchema = require("./subscriptionSchema");
const emailSchema = require("./emailSchema");

module.exports = {
  contactsSchema,
  updateFavoriteSchema,
  userSchema,
  subscriptionSchema,
  emailSchema,
};
